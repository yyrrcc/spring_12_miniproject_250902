<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycompany.mini.dao.MemberDao">

	<!-- 회원가입 -->
	<insert id="memSignUp">
		INSERT INTO minimember (memberid, password, name, phone, email) VALUES (#{memberId}, #{password}, #{name}, #{phone}, #{email})
	</insert>
	
	<!-- 로그인 -->
	<select id="memLogin" resultType="int">
		SELECT count(*) FROM minimember WHERE memberid = #{param1} AND password = #{param2}
	</select>
</mapper>
